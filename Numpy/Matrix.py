# Matrix in numpy 
# We can also use numpy array as matrix
import numpy as np
matrix = np.matrix([[1,2],[2,3],[3,4]])
print(matrix)
print(matrix.dtype)
print(type(matrix))
print(np.result_type(matrix))